include "main.h"

/**
* _abs - computes the absolute value of an integer
* @n: the int to check
* Return: the absolute value of int
*/
int _abs(int c)
{
	int c = a;
	int absvalue;


	if (c < 0)
	{
		c = c *(-1);
	}
	absvalue = c;
	return (absvalue);
}
